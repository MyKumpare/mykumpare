{
  "name": "Template",
  "type": "object",
  "properties": {
    "tenant_id": {
      "type": "string",
      "description": "ID of the firm (tenant) that owns this template record. Set on creation to the creator's linked_firm_id; drives tenant isolation via RLS."
    },
    "name": {
      "type": "string",
      "description": "Name of the template"
    },
    "template_type": {
      "type": "string",
      "description": "Type category of the template (managed via TemplateType master list)"
    },
    "create_date": {
      "type": "string",
      "format": "date",
      "description": "Date the template was created (auto-set to today, user-editable). Stored as YYYY-MM-DD."
    },
    "stages": {
      "type": "array",
      "description": "Due diligence stages for Manager Due Diligence templates (ordered list). Each stage has an id, a name, and optional sub-stages.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "sub_stages": {
            "type": "array",
            "description": "Ordered sub-stages within this stage",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "documentation_checklist": {
      "type": "array",
      "description": "Documentation checklist items defined in the template. Each item has an id and a name. When a due diligence is created from this template, these items are copied as the starting checklist for the user to attach documents, complete, and add notes.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          }
        }
      }
    },
    "approval_process_logic": {
      "type": "array",
      "description": "Approval process logic steps defined in the template. Each step references a due diligence stage (and optionally a sub-stage) to determine the workflow. When a due diligence is created from this template, these steps are copied as the starting approval process for the user to execute.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string",
            "description": "Name/description of the approval process step"
          },
          "stage_id": {
            "type": "string",
            "description": "ID of the referenced due diligence stage"
          },
          "stage_name": {
            "type": "string",
            "description": "Name of the referenced stage (denormalized for display)"
          },
          "sub_stage_id": {
            "type": "string",
            "description": "ID of the referenced sub-stage (optional, for finer-grained workflow)"
          },
          "sub_stage_name": {
            "type": "string",
            "description": "Name of the referenced sub-stage (denormalized for display)"
          }
        }
      }
    }
  },
  "required": [
    "name"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.tenant_id": "{{user.data.linked_firm_id}}"
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.tenant_id": "{{user.data.linked_firm_id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "$and": [
            {
              "data.tenant_id": "{{user.data.linked_firm_id}}"
            },
            {
              "$or": [
                {
                  "user_condition": {
                    "data.firm_role": "admin"
                  }
                },
                {
                  "user_condition": {
                    "data.firm_role": "co-admin"
                  }
                },
                {
                  "user_condition": {
                    "data.can_edit": true
                  }
                }
              ]
            }
          ]
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "$and": [
            {
              "data.tenant_id": "{{user.data.linked_firm_id}}"
            },
            {
              "$or": [
                {
                  "user_condition": {
                    "data.firm_role": "admin"
                  }
                },
                {
                  "user_condition": {
                    "data.firm_role": "co-admin"
                  }
                },
                {
                  "user_condition": {
                    "data.can_delete": true
                  }
                }
              ]
            }
          ]
        }
      ]
    }
  }
}