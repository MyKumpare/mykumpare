{
  "name": "VideoLibraryItem",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Video title (unique \u2014 validated for no duplicates)"
    },
    "description": {
      "type": "string",
      "description": "Optional description or summary of the video content"
    },
    "video_url": {
      "type": "string",
      "description": "URL of the video file (uploaded via UploadFile or an external URL)"
    },
    "thumbnail_url": {
      "type": "string",
      "description": "Optional thumbnail image URL for the video card"
    },
    "category": {
      "type": "string",
      "default": "How To Use",
      "description": "Category grouping for the video (e.g. 'How To Use', 'Best Practices', 'Training')"
    },
    "tag_ids": {
      "type": "array",
      "description": "Multi-select tag IDs assigned to this video. References VideoTag records. Users can assign multiple tags via the multi-select dropdown.",
      "items": {
        "type": "string"
      }
    },
    "tag_names": {
      "type": "array",
      "description": "Denormalized tag names for display (mirrors tag_ids, resolved from VideoTag records).",
      "items": {
        "type": "string"
      }
    },
    "linked_firm_ids": {
      "type": "array",
      "description": "IDs of investment firms this video/training manual is linked to. Lets users see relevant training videos directly from the firm summary page.",
      "items": {
        "type": "string"
      }
    },
    "linked_firm_names": {
      "type": "array",
      "description": "Denormalized firm names for display (mirrors linked_firm_ids).",
      "items": {
        "type": "string"
      }
    },
    "linked_dd_ids": {
      "type": "array",
      "description": "IDs of DueDiligence records this video/training manual is linked to. Lets users see relevant training videos directly from the due diligence project.",
      "items": {
        "type": "string"
      }
    },
    "linked_dd_names": {
      "type": "array",
      "description": "Denormalized due diligence names for display (mirrors linked_dd_ids).",
      "items": {
        "type": "string"
      }
    },
    "uploaded_by_name": {
      "type": "string",
      "description": "Display name of the user who uploaded/created this video (denormalized)"
    },
    "duration_label": {
      "type": "string",
      "description": "Optional human-readable duration (e.g. '3:45')"
    },
    "sort_order": {
      "type": "integer",
      "default": 0,
      "description": "Display order within the video library. Lower values appear first."
    }
  },
  "required": [
    "title",
    "video_url",
    "category"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "user"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": null,
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}