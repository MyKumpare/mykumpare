{
  "name": "CustomReport",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the custom report"
    },
    "description": {
      "type": "string",
      "description": "Free-text description of what the report should contain"
    },
    "data_source": {
      "type": "string",
      "enum": [
        "Firm",
        "Contact",
        "Product",
        "Portfolio",
        "Benchmark",
        "ContactActivity",
        "FollowUpTask"
      ],
      "description": "Primary data source entity"
    },
    "selected_fields": {
      "type": "array",
      "description": "Data fields to include in the report",
      "items": {
        "type": "string"
      }
    },
    "computations": {
      "type": "array",
      "description": "Computations to perform on the data",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "count",
              "sum",
              "average",
              "min",
              "max",
              "percentage",
              "growth"
            ]
          },
          "target_field": {
            "type": "string"
          },
          "group_by": {
            "type": "string"
          },
          "label": {
            "type": "string"
          }
        }
      }
    },
    "format_type": {
      "type": "string",
      "enum": [
        "table",
        "chart",
        "mixed"
      ],
      "description": "Primary report format"
    },
    "chart_type": {
      "type": "string",
      "enum": [
        "bar",
        "line",
        "pie",
        "area",
        "scatter"
      ],
      "description": "Chart visualization type"
    },
    "group_by": {
      "type": "string",
      "description": "Field to group results by"
    },
    "sort_by": {
      "type": "string",
      "description": "Field to sort results by"
    },
    "sort_order": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "default": "asc",
      "description": "Sort order"
    },
    "filters": {
      "type": "array",
      "description": "Structured field-based filters applied to scope the report to specific records (e.g. a single product by ID or name). Each filter is AND-combined.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique id for this filter row"
          },
          "field": {
            "type": "string",
            "description": "Entity field key to filter on (e.g. 'id', 'name', 'firm_id')"
          },
          "operator": {
            "type": "string",
            "enum": [
              "equals",
              "not_equals",
              "contains",
              "not_contains",
              "is_empty",
              "is_not_empty",
              "gt",
              "lt"
            ],
            "description": "Comparison operator"
          },
          "value": {
            "type": "string",
            "description": "Value to compare against (not used for is_empty / is_not_empty)"
          }
        },
        "required": [
          "field",
          "operator"
        ]
      }
    },
    "filters_description": {
      "type": "string",
      "description": "Free-text description of filters to apply (descriptive only; use the structured 'filters' array for actual filtering)"
    },
    "output_formats": {
      "type": "array",
      "description": "Available output formats",
      "items": {
        "type": "string",
        "enum": [
          "pdf",
          "csv",
          "print"
        ]
      }
    },
    "page_orientation": {
      "type": "string",
      "enum": [
        "portrait",
        "landscape"
      ],
      "default": "landscape",
      "description": "PDF page orientation"
    },
    "include_summary": {
      "type": "boolean",
      "default": true,
      "description": "Include a summary section"
    },
    "include_cover_page": {
      "type": "boolean",
      "default": false,
      "description": "Include a cover page in PDF"
    },
    "include_page_numbers": {
      "type": "boolean",
      "default": true,
      "description": "Include page numbers in PDF"
    }
  },
  "required": [
    "name",
    "data_source"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "user"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}