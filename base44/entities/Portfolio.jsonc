{
  "name": "Portfolio",
  "type": "object",
  "properties": {
    "firm_id": {
      "type": "string",
      "description": "ID of the associated allocator firm"
    },
    "allocator_name": {
      "type": "string",
      "description": "Name of the allocator firm (denormalized)"
    },
    "portfolio_name": {
      "type": "string",
      "description": "Name of the portfolio"
    },
    "inception_date": {
      "type": "string",
      "format": "date",
      "description": "Inception date of the portfolio"
    },
    "initial_allocation_amount": {
      "type": "number",
      "description": "Initial allocation amount for the portfolio"
    },
    "advisor_type": {
      "type": "string",
      "enum": [
        "Manager of Managers",
        "Investment Manager"
      ],
      "description": "Type of advisor managing this portfolio"
    },
    "advisor_firm_id": {
      "type": "string",
      "description": "ID of the advisor firm (MoM or IM)"
    },
    "advisor_firm_name": {
      "type": "string",
      "description": "Name of the advisor firm (denormalized)"
    },
    "advisor_inception_date": {
      "type": "string",
      "format": "date",
      "description": "Inception date of the advisor (MoM or IM) for this portfolio"
    },
    "advisor_initial_allocation_amount": {
      "type": "number",
      "description": "Initial allocation amount for the advisor (MoM or IM) for this portfolio"
    },
    "sub_managers": {
      "type": "array",
      "description": "Sub-manager products (only for Manager of Managers advisor type)",
      "items": {
        "type": "object",
        "properties": {
          "product_id": {
            "type": "string"
          },
          "product_name": {
            "type": "string"
          },
          "firm_name": {
            "type": "string"
          },
          "inception_date": {
            "type": "string",
            "format": "date",
            "description": "Inception date of this sub-manager in the portfolio"
          },
          "initial_allocation_amount": {
            "type": "number",
            "description": "Initial allocation amount for this sub-manager in the portfolio"
          }
        }
      }
    },
    "primary_benchmark_id": {
      "type": "string",
      "description": "ID of the primary benchmark for this portfolio"
    },
    "primary_benchmark_name": {
      "type": "string",
      "description": "Name of the primary benchmark (denormalized for display)"
    },
    "secondary_benchmarks": {
      "type": "array",
      "description": "Secondary benchmarks associated with this portfolio",
      "items": {
        "type": "object",
        "properties": {
          "benchmark_id": {
            "type": "string",
            "description": "ID of the benchmark"
          },
          "benchmark_name": {
            "type": "string",
            "description": "Name of the benchmark (denormalized for display)"
          }
        }
      }
    },
    "deleted_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the portfolio was soft-deleted"
    }
  },
  "required": [
    "firm_id",
    "portfolio_name",
    "inception_date"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "user"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": null,
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}