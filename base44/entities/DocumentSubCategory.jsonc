{
  "name": "DocumentSubCategory",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of a document sub-category (managed master list, reusable across documents)"
    }
  },
  "required": [
    "name"
  ]
}